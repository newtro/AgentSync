{
  "name": "skillmesh",
  "version": "0.1.1",
  "private": true,
  "description": "Git-driven, cross-harness skill synchronization",
  "type": "module",
  "bin": {
    "skillmesh": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/cli.js && node --test",
    "smoke": "node src/cli.js doctor --json"
  },
  "engines": {
    "node": ">=22"
  }
}
